# Copier ce fichier en .env.production et remplir les valeurs

# Base de données (Supabase, PlanetScale, Neon, etc.)
DATABASE_URL="postgresql://user:password@host:port/database"

# NextAuth Configuration
NEXTAUTH_URL="https://taplinkr.com"
NEXTAUTH_SECRET="générer avec: openssl rand -base64 32"

# Google OAuth (optionnel)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Email (optionnel)
EMAIL_SERVER="smtp://user:pass@smtp.example.com:587"
EMAIL_FROM="noreply@taplinkr.com"

# Upload
NEXT_PUBLIC_UPLOAD_URL="/uploads"

# Stripe (pour la monétisation - optionnel)
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""